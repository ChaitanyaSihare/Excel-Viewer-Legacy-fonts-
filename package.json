{
  "name": "yatharoop",
  "version": "1.0.0",
  "private": true,
  "description": "Legacy Hindi-font Excel viewer. Runs entirely client-side, no build step required for the app itself — TypeScript here is opt-in, used only for new modules going forward (see ts/README.md).",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "node tests/run.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
